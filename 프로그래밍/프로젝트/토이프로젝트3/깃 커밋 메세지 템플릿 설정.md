git config --global or --system commit.template [파일이름]


### 1) global로 적용하는 경우

`git config --global commit.template ~/.gitmessage.txt`

### 2) 현재 프로젝트에만 적용하는 경우

`git config commit.template .gitmessage.txt`